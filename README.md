# Video Stats Telegram Bot
Бот для анализа статистики видео с использованием AI для преобразования естественного языка в SQL-запросы

![Python](https://img.shields.io/badge/Python-3.11-blue)
![Postgres](https://img.shields.io/badge/PostgreSQL-17-blue)
![Redis](https://img.shields.io/badge/Redis-7-blue)
![Docker](https://img.shields.io/badge/Docker-ready-blue)

## Возможности
<h5>-Интеллектуальный анализ - преобразование вопросов на русском языке в SQL-запросы с помощью YandexGPT</h5>
<h5>-Кеширование результатов - автоматическое кеширование популярных запросов в Redis</h5>
<h5>-Автоматическая обработка данных - загрузка данных из JSON файлов в PostgreSQL</h5>

## Структура проекта
``` text
video-stats-bot/
├── app/                  # Основной файл запуска бота
│   ├── handlers/
│   │   ├── __init__.py
│   │   ├── base.py              # Базовые функции и фильтры
│   │   └── user_handlers.py              # Основные хендлеры пользователя
│   ├── services/
│   │   ├── gpt_service.py        # Сервис YandexGPT API
│   │   ├── db_service.py         # Сервис работы с PostgreSQL
│   │   └── cache_service.py     # Сервис кеширования Redis
│   └── core/
│       └── config.py            # Настройки приложения
├── bot/
│   └── bot.py                   # Точка входа (в контейнере) и основной файл для запуска бота
├── scripts/
│   └── load_json.py            # Скрипт загрузки данных
├── data/                       # Папка с JSON файлом
├── docs/                       # Папка для документации
├── docker-compose.yml          # Конфигурация Docker
├── Dockerfile                  # Образ Docker
├── .env                       # Конфигурация приложения
└── requirements.txt           # Зависимости Python
```
## Схема работы
<img src="https://github.com/VollpenTinger/tg-bot-video-analytics/blob/main/docs/diagram.png"> 

## Быстрый старт

### 1. Клонируйте репозиторий💿
```bash
git clone https://github.com/VollpenTinger/tg-bot-video-analytics
cd tg-bot-video-analytics
```

### 2. Создайте файл .env и отредактируйте подставив свои ключи и пароли⚙️ 
```
 cp .env.example .env
```

### 3. Запуск Docker 
```
bash docker-compose up -d --build
```
### 4. Готово✅
``` bash
docker-compose logs -f telegram-bot-id #просмотреть логи
```

## Тестирование

### 1. Установите зависимости(желательно в venv):
``` bash
pip install pytest pytest-asyncio pytest-mock
```

### 2.Запустите команду:
``` bash
pytest tests/ -v > logs/logs
```
## 🤝 Поддержка
### При возникновении проблем:

<h5> Проверьте логи: docker-compose logs -f</h5>

<h5> Убедитесь, что все переменные окружения установлены </h5>

<h5> Проверьте подключение к внешним API (Telegram, YandexGPT)</h5>

<h5>Откройте issue в репозитории проекта</h5>
